// define whether we are attached to the panel and at what distance (for the start of the game)
attached_to_paddle = true;
attached_to_paddle_distance = 30;

// define variables to define our speed and direction
speed_base = 5;
speed = 0;
direction = 0;
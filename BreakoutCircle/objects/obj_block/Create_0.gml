// define variables for the each blocks rotation angle, speed, speed increase when destroyed, and rotation radius from the center
rotate_angle = 0;
rotate_speed = 0;
rotate_speed_increase = 0.05;
rotate_radius = 100;

// set our base draw alpha (this changes when damaged)
draw_alpha = 1;

// define our hp values
hp_max = 2;
hp = hp_max;